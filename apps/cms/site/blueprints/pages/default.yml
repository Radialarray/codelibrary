title: Default Page

options:
  preview: "{{ kirby.option('frontendUrl') }}/{{ page.uri }}"


columns:
  main:
    width: 2/3
    sections:
      fields:
        type: fields
        fields:
          text:
            type: textarea
            size: huge
  sidebar:
    width: 1/3
    sections:
      pages:
        type: pages
        template: layout
      files:
        type: files

