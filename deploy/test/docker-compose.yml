version: '3.6'

services:
  nginx:
    image: nginx:latest
    volumes:
      - nas-share:/tmp

volumes:
  nas-share:
    driver_opts:
      type: cifs
      o: "username=codelibrary,password=j3o4ij()kjnklsd!"
      device: "//172.17.172.21/Share_CodeLibrary"


# volumes:
#   codelibrary:
#     name: codelibrary
#     driver: local
#     driver_opts:
#       type: 'cifs'
#       device: '//${BACKEND_SHARE_REMOTE_DOMAIN}/${BACKEND_SHARE_PATH_TO_LIBRARY}'
#       o: 'addr=${BACKEND_SHARE_REMOTE_IP},username=${BACKEND_SHARE_USER},password=${BACKEND_SHARE_PASSWORD}'
