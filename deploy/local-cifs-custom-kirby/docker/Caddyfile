:80 {
        root * /srv
        encode gzip
        tls internal
        file_server
        # php_fastcgi cms_php_fpm:9000

        php_fastcgi / /var/run/php-fpm.sock php
        # php_fastcgi / 127.0.0.1:9000 php

        # rewrite {
        #         to {path} /index.php?{query}
        # }


        # log stdout
        # errors stdout
        # on startup php-fpm --nodaemonize
}